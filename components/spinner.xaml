<UserControl
	x:Class="Logi.Spinner" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:logi="clr-namespace:Logi">
	<Grid
		Width="32"
		Height="32"
		RenderTransformOrigin="0.5,0.5">
		<Image
			Width="0"
			Height="0"
			Source="pack://application:,,,/Resources/images/spinner.png"
			RenderTransformOrigin="0.5,0.5"
			Style="{StaticResource Spinner}">
			<UIElement.RenderTransform>
				<RotateTransform
					x:Name="AnimatedRotateTransform"
					Angle="0" />
			</UIElement.RenderTransform>
			<FrameworkElement.Triggers>
				<EventTrigger
					RoutedEvent="Loaded">
					<BeginStoryboard>
						<Storyboard>
							<DoubleAnimation
								Storyboard.TargetProperty="(Rectangle.RenderTransform).(RotateTransform.Angle)"
								To="360"
								Duration="0:0:1"
								RepeatBehavior="Forever" />
						</Storyboard>
					</BeginStoryboard>
				</EventTrigger>
			</FrameworkElement.Triggers>
		</Image>
	</Grid>
</UserControl>
